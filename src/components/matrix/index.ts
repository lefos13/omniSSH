export {
  MatrixBackground,
  MATRIX_DEFAULTS,
  STORAGE_PALETTE_KEY,
  STORAGE_SPEED_KEY,
  STORAGE_BRIGHTNESS_KEY,
  STORAGE_SIZE_KEY,
  STORAGE_HEAD_KEY,
} from "./MatrixBackground";
export type {
  ColorTheme as MatrixColorTheme,
  GlyphSize as MatrixGlyphSize,
  HeadMode as MatrixHeadMode,
} from "./MatrixBackground";
